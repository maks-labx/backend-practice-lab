# Status Counter

Small Python exercise for practicing dictionaries, loops, functions, and unit tests.

## Task

Given a list of inspection statuses, count how many times each status appears.

Example:

```python
["OK", "PROBLEM", "OK", "PROBLEM", "OK"]